import React from 'react';
import styles from './styles.module.css';

const Notification = () => {
    return (
        <div className={styles.wrapper}>
            Free delivery for all orders over $50. Order food now!
        </div>
    );
};

export { Notification };
